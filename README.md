# 家宴点菜小程序

一个专门用于家宴点菜的微信小程序，支持多菜系（中餐、日料、西餐、火锅等），让客人提前选菜，让主人轻松准备。

## ✨ 功能特点

### 🍽️ 用户端

- **浏览菜单**：支持多菜系分类（中餐、日料、西餐、火锅等）
- **菜品详情**：查看菜品图片、描述、所需食材
- **购物车**：自由添加、修改菜品数量
- **提交订单**：填写拜访信息和特殊要求

### 👨‍🍳 管理员端

- **菜单管理**：增删改查菜品，上传菜品图片
- **多菜系支持**：灵活管理不同菜系和分类
- **订单查看**：查看所有客人的点菜订单
- **食材清单**：自动汇总需要准备的食材
- **一键复制**：复制购物清单到剪贴板

## 📁 项目结构

```
family-menu-miniprogram/
├── cloudfunctions/          # 云函数
│   ├── getDishes/           # 获取菜品列表
│   ├── getDishDetail/       # 获取菜品详情
│   ├── getCategories/       # 获取分类列表
│   ├── getCuisines/         # 获取菜系列表
│   ├── addDish/             # 添加菜品
│   ├── updateDish/          # 更新菜品
│   ├── updateDishImages/    # 更新菜品图片
│   ├── deleteDish/          # 删除菜品
│   ├── createOrder/         # 创建订单
│   ├── getOrders/           # 获取订单列表
│   ├── deleteOrder/         # 删除订单
│   └── getIngredients/      # 获取食材清单
├── database_init/           # 数据库初始化数据
│   ├── cuisine.json         # 菜系数据
│   ├── cuisine_category.json # 分类数据
│   └── dish.json            # 菜品示例数据
├── miniprogram/             # 小程序前端
│   ├── pages/               # 页面
│   │   ├── home/            # 主页
│   │   ├── dishList/        # 菜品列表
│   │   ├── dishDetail/      # 菜品详情
│   │   ├── cart/            # 购物车
│   │   ├── orderConfirm/    # 订单确认
│   │   ├── admin/           # 管理员主页
│   │   ├── menuManage/      # 菜单管理
│   │   ├── dishEdit/        # 编辑菜品
│   │   ├── orderList/       # 订单列表
│   │   ├── orderDetail/     # 订单详情
│   │   └── ingredientList/  # 食材清单
│   ├── images/              # 图片资源
│   ├── config/              # 配置文件
│   ├── app.js
│   ├── app.json
│   └── app.wxss
└── README.md                # 项目说明

```

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/fightforbelief/family-menu-miniprogram.git
cd family-menu-miniprogram
```

### 2. 打开项目

使用微信开发者工具打开项目目录。

### 3. 配置云开发

1. 点击微信开发者工具中的"云开发"按钮
2. 开通云开发环境（如果还没有）
3. 记下你的环境 ID（格式类似：`your-env-id`）

### 4. 修改配置文件

修改以下文件中的云环境 ID：

**miniprogram/app.js**
```javascript
wx.cloud.init({
  env: 'your-env-id', // 替换为你的环境 ID
  traceUser: true
})
```

**miniprogram/config/cloudConfig.js**
```javascript
module.exports = {
  // 上传默认图片后，替换为你的云存储 File ID
  DEFAULT_DISH_IMAGE: 'cloud://your-env-id.xxxx/dish_image/default-dish.png'
}
```

**project.config.json**
```json
{
  "cloudfunctionConfig": {
    "env": "your-env-id" // 替换为你的环境 ID
  }
}
```

### 5. 创建数据库集合

在云开发控制台创建以下集合：

- `cuisines`（菜系表）
- `cuisine_category`（菜系分类表）
- `dish`（菜品表）
- `orders`（订单表）

#### 导入初始数据

可以使用 `database_init/` 目录下的 JSON 文件导入初始数据：

1. 在云开发控制台选择对应的集合
2. 点击"导入"
3. 选择对应的 JSON 文件

### 6. 上传云函数

右键点击每个云函数文件夹（在 cloudfunctions/ 目录下），选择"上传并部署：云端安装依赖"。

需要上传的云函数：
- getDishes
- getDishDetail
- getCategories
- getCuisines
- addDish
- updateDish
- updateDishImages
- deleteDish
- createOrder
- getOrders
- deleteOrder
- getIngredients

### 7. 上传默认图片

1. 在云开发控制台的"云存储"中创建文件夹 `dish_image/`
2. 上传 `miniprogram/images/default-dish.png` 作为默认菜品图片
3. 获取该图片的 File ID，更新到 `miniprogram/config/cloudConfig.js`

### 8. 编译运行

点击"编译"按钮，在模拟器中预览效果。

## 📖 使用说明

### 用户使用流程

1. 打开小程序，点击"开始点菜"
2. 选择菜系（中餐、日料、西餐、火锅等）
3. 浏览菜品列表，点击菜品查看详情
4. 添加喜欢的菜品到购物车
5. 点击"去确认"，填写拜访信息
6. 提交订单，等待美味佳肴 😋

### 管理员使用流程

1. 打开小程序，点击"管理员模式"
2. 输入管理员密码（**需要自行在代码中设置**）
3. 进入管理后台，可以：
   - **菜单管理**：添加、编辑、删除菜品
   - **订单查看**：查看所有客人的点菜订单
   - **食材清单**：查看需要准备的食材，一键复制

## 🗄️ 数据库设计

### 数据库集合

#### 1. cuisines（菜系表）

存储所有支持的菜系类型（中餐、日料、西餐、火锅等）

**字段说明**：
- `cuisine` (string)：菜系名称，如"中餐"、"日料"
- `cuisine_code` (string)：菜系唯一编码，如 CN、JP、WEST
- `sort_order` (number)：展示排序（数值越小越靠前）

**索引**：
```javascript
{ "cuisine_code": 1 }  // 唯一索引
{ "sort_order": 1 }
```

**示例数据**：
```json
{
  "cuisine": "中餐",
  "cuisine_code": "CN",
  "sort_order": 10
}
```

---

#### 2. cuisine_category（菜系分类表）

存储每个菜系下的分类（如中餐的凉菜、热菜、主食等）

**字段说明**：
- `cuisine_code` (string)：所属菜系编码（外键）
- `category` (string)：分类名称，如"凉菜"、"热菜"
- `category_code` (string)：分类唯一编码，如 CN_COLD_DISHES
- `sort_order` (number)：该菜系下的展示排序

**索引**：
```javascript
{ "category_code": 1 }  // 唯一索引
{ "cuisine_code": 1, "sort_order": 1 }  // 复合索引
```

**示例数据**：
```json
{
  "cuisine_code": "CN",
  "category": "凉菜",
  "category_code": "CN_COLD_DISHES",
  "sort_order": 10
}
```

---

#### 3. dish（菜品表）

存储所有菜品的详细信息

**字段说明**：
- `name` (string)：菜品名称
- `cuisine_code` (string)：菜系编码（外键）
- `category_code` (string)：分类编码（外键）
- `description` (string)：菜品描述（可选）
- `image` (string)：图片 URL 或 File ID（默认 "default"）
- `ingredients` (array)：食材列表，如 ["鸡腿肉","花生","青椒"]
- `isAvailable` (boolean)：是否上架可点

**索引**：
```javascript
{ "cuisine_code": 1 }
{ "category_code": 1 }
{ "isAvailable": 1 }
{ "name": "text" }  // 文本索引，用于搜索
```

**示例数据**：
```json
{
  "name": "宫保鸡丁",
  "cuisine_code": "CN",
  "category_code": "CN_HOT_DISHES",
  "description": "可选辣度",
  "image": "cloud://xxx.png",
  "ingredients": ["鸡腿肉", "花生", "青椒", "干辣椒"],
  "isAvailable": true
}
```

---

#### 4. orders（订单表）

存储客人的点菜订单

**字段说明**：
- `guestName` (string)：客人姓名（可选）
- `guestPhone` (string)：客人电话（可选）
- `visitDate` (string)：拜访日期（格式：YYYY-MM-DD）
- `visitTime` (string)：拜访时间（格式：HH:mm）
- `dishes` (array)：点的菜品数组
  - `dishId`：菜品 ID
  - `dishName`：菜品名称
  - `cuisine_code`：菜系编码
  - `category_code`：分类编码
  - `quantity`：数量
- `totalDishes` (number)：总菜品数
- `specialRequests` (string)：特殊要求
- `status` (string)：订单状态（pending/confirmed/completed/cancelled）
- `createTime` (date)：创建时间
- `updateTime` (date)：更新时间

**索引**：
```javascript
{ "visitDate": 1, "status": 1 }
{ "createTime": -1 }
```

**示例数据**：
```json
{
  "guestName": "张三",
  "guestPhone": "13800138000",
  "visitDate": "2026-01-20",
  "visitTime": "18:00",
  "dishes": [
    {
      "dishId": "dish_001",
      "dishName": "宫保鸡丁",
      "cuisine_code": "CN",
      "category_code": "CN_HOT_DISHES",
      "quantity": 2
    }
  ],
  "totalDishes": 2,
  "specialRequests": "不要太辣",
  "status": "pending",
  "createTime": "2026-01-16T10:00:00.000Z",
  "updateTime": "2026-01-16T10:00:00.000Z"
}
```

---

### 数据关系图

```
cuisines (菜系)
  ↓ (1:N)
cuisine_category (分类)
  ↓ (1:N)
dish (菜品)
  ↓ (N:M)
orders (订单)
```

---

### 查询示例

#### 获取某菜系的所有分类
```javascript
db.collection('cuisine_category')
  .where({
    cuisine_code: 'CN'
  })
  .orderBy('sort_order', 'asc')
  .get()
```

#### 获取某分类的所有菜品
```javascript
db.collection('dish')
  .where({
    category_code: 'CN_COLD_DISHES',
    isAvailable: true
  })
  .get()
```

#### 搜索菜品
```javascript
db.collection('dish')
  .where({
    name: db.RegExp({
      regexp: '.*鸡.*',
      options: 'i'
    })
  })
  .get()
```

## 🛠️ 技术栈

- **前端**：微信小程序原生开发
- **后端**：微信云开发（云函数 + 云数据库 + 云存储）
- **数据库**：云开发 NoSQL 数据库
- **存储**：云存储（图片上传）

## 🎨 自定义配置

### 修改管理员密码

编辑 `miniprogram/pages/home/home.js`：
```javascript
checkPassword(password) {
  // 修改为你自己的密码
  if (password === 'your-password') {
    wx.navigateTo({
      url: '/pages/admin/admin'
    })
  } else {
    wx.showToast({
      title: '密码错误',
      icon: 'none'
    })
  }
}
```

### 修改主题颜色

编辑 `miniprogram/app.wxss` 和各页面的 wxss 文件。

### 添加更多菜系

1. 在云数据库的 `cuisines` 集合中添加新菜系
2. 在 `cuisine_category` 集合中添加对应的分类
3. 在菜品管理中添加该菜系的菜品

## 🔒 权限说明

### 开发环境权限建议

- **cuisines**: 所有用户可读，仅管理员可写
- **cuisine_category**: 所有用户可读，仅管理员可写
- **dish**: 所有用户可读，仅管理员可写
- **orders**: 所有用户可读写

### 生产环境

建议使用云函数进行所有数据库操作，前端不直接访问数据库，提高安全性。

## 📝 待办事项 & 扩展建议

- [ ] 添加用户登录系统
- [ ] 菜品评价功能
- [ ] 菜品价格和规格管理
- [ ] 详细的烹饪步骤
- [ ] 营养成分展示
- [ ] 订阅消息通知
- [ ] 数据统计分析
- [ ] 多语言支持

## ❓ 常见问题

**Q: 如何修改管理员密码？**  
A: 编辑 `miniprogram/pages/home/home.js` 文件中的密码判断逻辑。

**Q: 如何添加更多菜系？**  
A: 在云数据库的 `cuisines` 和 `cuisine_category` 集合中添加数据。

**Q: 云函数调用失败怎么办？**  
A: 检查云函数是否已上传并部署成功，查看云开发控制台的日志。

**Q: 如何更换默认菜品图片？**  
A: 上传图片到云存储，获取 File ID，更新到 `miniprogram/config/cloudConfig.js`。

## 📄 开源协议

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📧 联系方式

如有问题或建议，欢迎提交 Issue。

---

**享受烹饪，享受生活！** 🍳✨
